# 课表日程助手 设计文档（2026-09-06，已获用户批准）

## 需求（用户原话归纳）
电脑 App：记录课表、日程，能弹窗提醒，UI 高级，后台占用小。

## 增补需求（2026-09-06 用户确认）
- 天气：加。Open-Meteo 免费接口（无 key），设置页选城市（geocoding 缓存经纬度），主页头部显示今日天气；境内直连性能待实测，超时静默降级显示 --
- 日历：加公农历显示。纯本地农历算法（1900-2100 表）+ 传统节日/公历节日名；不显示法定调休安排（逐年变动，避免不可靠数据）
- 网购平台接入：不做（平台 API 不对个人开放，爬虫违规有封号风险）

## 已确认决策
- 技术栈：PySide6 (Qt) + SQLite，venv 隔离，PyInstaller 打包
- 数据：首次导入课表 xlsx（用户微信收到的那份格式），之后 App 内手动增改；日程手动添加
- UI：Win11 Fluent 风（Mica 质感、圆角卡片、跟随系统深浅色）
- 弹窗提醒：自绘右下角 toast（不依赖第三方 toast 库），默认提前 10 分钟可配
- 开机自启：要，默认关（写 HKCU\Software\Microsoft\Windows\CurrentVersion\Run，可随时关回滚）
- 单实例运行；关闭主窗口 → 缩托盘不退出

## 架构
```
main.py         入口：单实例锁 → QApplication → 托盘 + 主窗口
db.py           SQLite 三张表 courses/events/settings + markers，data/app.db
importer.py     openpyxl 解析课表 xlsx → 导入预览 → 写库
reminder.py     纯函数 next_reminders(now, ...) 可单测；QTimer 每分钟轮询
ui/             主窗口（课表周视图/日程列表/设置三页）+ QSS 两套主题
toast.py        无边框圆角置顶 toast，QPropertyAnimation 淡入，点击开主窗口
tray.py         QSystemTrayIcon：打开/暂停提醒/退出
autostart.py    注册表 Run 键读写（默认关）
icon.py         纯标准库画 ICO（托盘/窗口/exe 共用）
```

## 关键机制
- 周次：设置项 first_monday（默认 2026-09-07）；week = (today - first_monday).days // 7 + 1
- 小节→时间映射默认表（1~12 节，45 分钟/节，设置可改 JSON）：
  1=08:00 2=08:55 3=10:00 4=10:55 5=11:50 6=14:00 7=14:55 8=16:00 9=16:55 10=19:00 11=19:55 12=20:50
- 课程条目存 weekday/section_start/section_end/weeks("9-12;13-14" 形式)，提醒只在该周该小节开始前 N 分钟触发
- 占周不占时间（课程设计/工程训练）：markers 表，课表页底部展示，不设时点提醒
- 导入覆盖前自动备份 data/app.db → app.backup.db

## 数据安全与回滚
- 数据单文件 data/app.db，删除即清空，无其他系统痕迹
- 自启注册表键值名 KebiaoReminder，删除键即回滚
- 程序不含服务/驱动/计划任务

## 里程碑
1. db + importer（真实 xlsx 验证）
2. reminder 引擎（单测周次与触发）
3. UI 三页 + Fluent QSS
4. 托盘 + toast + 单实例 + 自启
5. 打包 onedir exe
